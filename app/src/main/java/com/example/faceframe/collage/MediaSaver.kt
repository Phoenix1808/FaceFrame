package com.example.faceframe.collage

//Do kaam: gallery mein save (MediaStore — Android 10+ pe scoped storage, neeche WRITE_EXTERNAL_STORAGE), aur share (cache mein likho → FileProvider se URI → ACTION_SEND)
class MediaSaver {
}